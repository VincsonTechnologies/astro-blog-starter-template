export default {
  async fetch(request) {
    return new Response(`
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Vincson Technologies</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      min-height: 100vh;
      font-family: Arial, sans-serif;
      color: white;
      background:
        radial-gradient(circle at top left, rgba(125, 55, 255, 0.35), transparent 35%),
        radial-gradient(circle at bottom right, rgba(85, 0, 255, 0.25), transparent 30%),
        linear-gradient(135deg, #05050a 0%, #0b0614 45%, #120a22 100%);
      overflow-x: hidden;
    }

    .grid {
      position: fixed;
      inset: 0;
      background-image:
        linear-gradient(rgba(255,255,255,0.04) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255,255,255,0.04) 1px, transparent 1px);
      background-size: 40px 40px;
      mask-image: linear-gradient(to bottom, rgba(0,0,0,0.9), rgba(0,0,0,0.25));
      pointer-events: none;
    }

    .glow {
      position: fixed;
      width: 700px;
      height: 700px;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(131, 56, 236, 0.22), transparent 65%);
      top: -180px;
      right: -180px;
      filter: blur(30px);
      pointer-events: none;
    }

    .wrap {
      position: relative;
      z-index: 2;
      max-width: 1100px;
      margin: 0 auto;
      padding: 80px 24px;
    }

    .hero {
      padding: 90px 0 50px;
      max-width: 720px;
    }

    .eyebrow {
      display: inline-block;
      font-size: 14px;
      letter-spacing: 2px;
      text-transform: uppercase;
      color: #b9a7ff;
      margin-bottom: 18px;
    }

    h1 {
      font-size: clamp(40px, 7vw, 76px);
      line-height: 1.02;
      margin-bottom: 20px;
    }

    .accent {
      color: #b18cff;
    }

    .subtext {
      font-size: 18px;
      line-height: 1.7;
      color: #d6d0e8;
      max-width: 620px;
      margin-bottom: 34px;
    }

    .buttons {
      display: flex;
      flex-wrap: wrap;
      gap: 14px;
      margin-bottom: 40px;
    }

    .btn {
      text-decoration: none;
      padding: 14px 20px;
      border-radius: 12px;
      font-weight: 700;
      transition: 0.2s ease;
      display: inline-block;
    }

    .btn-primary {
      background: linear-gradient(135deg, #7b2cff, #a855f7);
      color: white;
    }

    .btn-secondary {
      border: 1px solid rgba(255,255,255,0.14);
      color: white;
      background: rgba(255,255,255,0.04);
      backdrop-filter: blur(8px);
    }

    .cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 18px;
      margin-top: 30px;
    }

    .card {
      padding: 22px;
      border-radius: 18px;
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.08);
      backdrop-filter: blur(10px);
    }

    .card h3 {
      font-size: 18px;
      margin-bottom: 10px;
    }

    .card p {
      color: #d1cbe2;
      line-height: 1.6;
      font-size: 15px;
    }

    .section-title {
      margin-top: 70px;
      margin-bottom: 18px;
      font-size: 28px;
    }

    .footer {
      margin-top: 70px;
      color: #9f96b7;
      font-size: 14px;
    }
  </style>
</head>
<body>
  <div class="grid"></div>
  <div class="glow"></div>

  <div class="wrap">
    <section class="hero">
      <div class="eyebrow">Vincson Technologies</div>
      <h1>AI-Powered <span class="accent">Cybersecurity</span> for Small Business</h1>
      <p class="subtext">
        Modern protection, threat visibility, and security guidance designed for organizations that need stronger defense without enterprise complexity.
      </p>

      <div class="buttons">
        <a class="btn btn-primary" href="mailto:youremail@example.com">Contact Us</a>
        <a class="btn btn-secondary" href="#services">View Services</a>
      </div>
    </section>

    <h2 id="services" class="section-title">Core Services</h2>
    <div class="cards">
      <div class="card">
        <h3>Threat Monitoring</h3>
        <p>Ongoing visibility into suspicious activity, weak points, and signs your environment may be exposed.</p>
      </div>
      <div class="card">
        <h3>Security Assessments</h3>
        <p>Identify gaps in your network, accounts, devices, and business tools before attackers do.</p>
      </div>
      <div class="card">
        <h3>AI-Enhanced Defense</h3>
        <p>Use modern detection and response workflows to keep pace with increasingly automated threats.</p>
      </div>
    </div>

    <div class="footer">
      © Vincson Technologies
    </div>
  </div>
</body>
</html>
    `, {
      headers: { "content-type": "text/html;charset=UTF-8" },
    });
  },
};
